spring.application.name=finance-dashboard

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/finance_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:King2580}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=${PORT:8080}

jwt.secret=${JWT_SECRET:mysecretkeymysecretkeymysecretkey12345}
jwt.expiration=86400000